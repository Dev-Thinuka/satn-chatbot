# --- Core Web Framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# --- Data & Database ---
SQLAlchemy>=2.0.29
psycopg[binary]>=3.1.18           # PostgreSQL driver for SQLAlchemy (postgresql+psycopg://)

# --- Pydantic & Settings ---
pydantic>=2.6.4
pydantic-settings>=2.2.1

# --- Auth & Security ---
python-jose[cryptography]>=3.3.0  # JWT handling
passlib[bcrypt]>=1.7.4            # password hashing
email-validator>=2.2.0            # FastAPI EmailStr support

# --- Migrations (optional but recommended) ---
alembic>=1.13.1

# --- HTTP / External APIs (LLM, notifications, etc.) ---
httpx>=0.27.0
requests>=2.32.0

# --- LLM / Chatbot Integration ---
openai>=1.30.0

# --- PDF & Document Utilities (for pdf_service, pdfs, etc.) ---
pypdf>=4.2.0

# --- Env & Logging ---
python-dotenv>=1.0.1
loguru>=0.7.2

# --- Testing & Dev Tools ---
pytest>=8.1.0
pytest-asyncio>=0.23.0
httpx>=0.27.0           # already above, but fine if duplicated
coverage>=7.5.0
black>=24.4.0
isort>=5.13.0
mypy>=1.9.0
ruff>=0.4.0
